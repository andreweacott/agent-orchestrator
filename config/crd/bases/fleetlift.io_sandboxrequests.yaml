---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: sandboxrequests.fleetlift.io
spec:
  group: fleetlift.io
  names:
    kind: SandboxRequest
    listKind: SandboxRequestList
    plural: sandboxrequests
    singular: sandboxrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.podName
      name: Pod
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SandboxRequest is the Schema for the sandboxrequests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SandboxRequestSpec defines the desired state of a sandbox
            properties:
              credentials:
                description: Credentials references secrets to mount in the sandbox
                items:
                  description: CredentialRef references a secret to mount in the sandbox
                  properties:
                    envVar:
                      description: EnvVar is the environment variable name to use
                        (for single-key secrets)
                      type: string
                    mountPath:
                      description: |-
                        MountPath is the path where the secret should be mounted
                        If not specified, secret is injected as environment variables
                      type: string
                    name:
                      description: Name of the secret
                      type: string
                    secretKey:
                      description: SecretKey is the specific key from the secret to
                        use
                      type: string
                  required:
                  - name
                  type: object
                type: array
              env:
                additionalProperties:
                  type: string
                description: Env contains environment variables to set in the sandbox
                type: object
              execRequests:
                description: |-
                  ExecRequests contains exec requests from the worker
                  Worker appends new requests, controller processes them
                items:
                  description: ExecRequest represents a command execution request
                    from the worker
                  properties:
                    command:
                      description: Command is the command and arguments to execute
                      items:
                        type: string
                      type: array
                    env:
                      additionalProperties:
                        type: string
                      description: Env contains additional environment variables for
                        this command
                      type: object
                    id:
                      description: ID is a unique identifier for this exec request
                        (UUID)
                      type: string
                    timeoutSeconds:
                      description: TimeoutSeconds is the maximum time allowed for
                        the command (default 300, max 1800)
                      format: int32
                      type: integer
                    user:
                      description: User to run the command as
                      type: string
                    workingDir:
                      description: WorkingDir is the working directory for the command
                      type: string
                  required:
                  - command
                  - id
                  type: object
                type: array
              image:
                description: Image is the container image to use for the sandbox
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector for pod scheduling
                type: object
              resources:
                description: Resources defines resource limits for the sandbox
                properties:
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Limits describes the maximum amount of compute resources
                      allowed
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Requests describes the minimum amount of compute
                      resources required
                    type: object
                type: object
              runtimeClassName:
                description: RuntimeClassName specifies the RuntimeClass (e.g., gvisor)
                type: string
              taskId:
                description: TaskID is the unique identifier for this sandbox task
                type: string
              ttlSecondsAfterFinished:
                description: TTLSecondsAfterFinished specifies how long to keep the
                  sandbox after completion
                format: int32
                type: integer
              workingDir:
                description: WorkingDir is the working directory inside the container
                type: string
            required:
            - image
            - taskId
            type: object
          status:
            description: SandboxRequestStatus defines the observed state of a SandboxRequest
            properties:
              completionTime:
                description: CompletionTime is when the sandbox finished
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the sandbox's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              execResults:
                description: ExecResults contains results of processed exec requests
                items:
                  description: ExecResult contains the result of an exec request
                  properties:
                    completedAt:
                      description: CompletedAt is when the exec completed
                      format: date-time
                      type: string
                    error:
                      description: Error contains any error message from the exec
                        operation
                      type: string
                    exitCode:
                      description: ExitCode is the command's exit code
                      type: integer
                    id:
                      description: ID matches the ExecRequest ID
                      type: string
                    stderr:
                      description: Stderr contains the command's standard error (truncated
                        to 1MB)
                      type: string
                    stdout:
                      description: Stdout contains the command's standard output (truncated
                        to 1MB)
                      type: string
                  required:
                  - exitCode
                  - id
                  type: object
                type: array
              jobName:
                description: JobName is the name of the Job created for this sandbox
                type: string
              lastExecProcessedIndex:
                description: |-
                  LastExecProcessedIndex is the index of the last processed exec request
                  Controller uses this to track which requests have been handled
                type: integer
              message:
                description: Message provides additional status information
                type: string
              phase:
                description: Phase represents the current lifecycle phase of the sandbox
                enum:
                - Pending
                - Provisioning
                - Running
                - Succeeded
                - Failed
                type: string
              podName:
                description: PodName is the name of the Pod running the sandbox
                type: string
              startTime:
                description: StartTime is when the sandbox started running
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
